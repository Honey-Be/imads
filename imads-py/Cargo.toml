[package]
name = "imads-py"
version = "2.1.0"
edition = "2024"
license = "MIT OR Apache-2.0 OR LGPL-2.1-or-later"

[lib]
name = "imads"
crate-type = ["cdylib"]

[dependencies]
imads-core = { path = "../imads-core" }
pyo3 = { version = "0.28.3", features = ["extension-module", "macros"] }
