[package]
name = "imads-jni"
version = "1.0.2"
edition = "2024"
license = "MIT OR Apache-2.0 OR LGPL-2.1-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
imads-core = { path = "../imads-core" }
jni = "0.21"
