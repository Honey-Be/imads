[package]
name = "imads-ffi"
version = "1.0.1"
edition = "2024"
license = "MIT OR Apache-2.0 OR LGPL-2.1-or-later"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
imads-core = { path = "../imads-core" }
